# 图片版权存证系统 🖼️⛓️

[![Solidity](https://img.shields.io/badge/Solidity-0.8.21-363636?logo=solidity)](https://soliditylang.org/)
[![React](https://img.shields.io/badge/React-18.2.0-61DAFB?logo=react)](https://react.dev/)
[![Truffle](https://img.shields.io/badge/Truffle-5.11.5-5E464D?logo=truffle)](https://trufflesuite.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

基于区块链的去中心化图片版权保护系统，将图片存储在 IPFS 上，并通过以太坊智能合约记录版权信息，实现不可篡改的版权存证和验证。

## ✨ 核心特性

- 🔐 **版权保护**: 图片 IPFS 哈希上链，永久记录版权归属
- 🌐 **去中心化存储**: 使用 IPFS 存储图片，避免单点故障
- ⚡ **快速验证**: 通过 IPFS 哈希即时验证图片版权
- 🎯 **权限管理**: 只有作者可以更新自己的作品信息
- 📊 **多维查询**: 支持按 ID、作者、哈希等多种方式查询
- 🎨 **现代化界面**: React + TailwindCSS 构建的响应式 Web 应用
- 🔄 **实时更新**: 基于区块链事件的实时 UI 更新

## 🛠️ 技术栈

### 区块链层
- **智能合约**: Solidity ^0.8.21
- **开发框架**: Truffle v5.11.5
- **测试网络**: Ganache (本地以太坊)
- **测试框架**: Truffle Test (Mocha + Chai)

### 存储层
- **去中心化存储**: IPFS Desktop
- **IPFS 客户端**: ipfs-http-client ^60.0.1

### 前端层
- **框架**: React 18.2.0
- **构建工具**: Vite 5.2.0
- **Web3 库**: Ethers.js 6.11.1
- **样式**: TailwindCSS 3.4.1
- **图标**: Lucide React 0.344.0
- **语言**: JavaScript (ES6+)

## 📁 项目结构

```
Image-copyright-on-chain/
├── contracts/                      # 智能合约
│   ├── ImageCopyright.sol         # 核心版权存证合约
│   └── .gitkeep
├── migrations/                     # 合约部署脚本
│   ├── 1_deploy_contracts.js      # 部署配置
│   └── .gitkeep
├── test/                          # 合约测试
│   ├── ImageCopyright.test.js     # 完整测试套件
│   └── .gitkeep
├── scripts/                       # 工具脚本
│   ├── interact.js                # 合约交互示例
│   └── check_connect.js           # 连接检查
├── client/                        # React 前端应用
│   ├── src/
│   │   ├── components/            # React 组件
│   │   │   ├── UploadImage.jsx   # 图片上传组件
│   │   │   ├── ImageGallery.jsx  # 图片列表组件
│   │   │   └── VerifyCopyright.jsx # 版权验证组件
│   │   ├── config/
│   │   │   └── contract.js       # 合约配置 (ABI & 地址)
│   │   ├── services/
│   │   │   └── copyrightService.js # 业务逻辑服务
│   │   ├── utils/
│   │   │   ├── contract.js       # 合约工具函数
│   │   │   └── ipfs.js           # IPFS 工具函数
│   │   ├── App.jsx               # 主应用组件
│   │   ├── main.jsx              # 应用入口
│   │   └── index.css             # 全局样式
│   ├── index.html                # HTML 模板
│   ├── vite.config.js            # Vite 配置
│   ├── tailwind.config.js        # TailwindCSS 配置
│   ├── postcss.config.js         # PostCSS 配置
│   └── package.json              # 前端依赖
├── docs/                          # 文档
│   └── FRONTEND_INTEGRATION.md   # 前端集成指南
├── truffle-config.js              # Truffle 配置
├── package.json                   # 后端依赖
├── QUICKSTART.md                  # 快速入门指南
└── README.md                      # 本文件
```

## 智能合约功能

### 核心功能

1. **上传图片版权信息** (`uploadImage`)
   - 存储 IPFS 哈希、标题、描述
   - 自动记录作者地址和时间戳
   - 防止重复上传同一图片

2. **查询功能**
   - `getAllImages()`: 获取所有图片
   - `getImage(id)`: 根据ID查询图片
   - `getImagesByAuthor(address)`: 查询指定作者的所有图片
   - `getImageByHash(ipfsHash)`: 根据IPFS哈希查询图片（用于版权验证）

3. **版权验证**
   - `verifyImageHash(ipfsHash)`: 验证图片是否已上链

4. **更新功能**
   - `updateImageInfo()`: 作者可更新图片标题和描述

5. **统计功能**
   - `getStats()`: 获取总图片数和用户图片数

### 数据结构

```solidity
struct Image {
    uint256 id;              // 图片唯一ID
    string ipfsHash;         // IPFS哈希值(CID)
    string title;            // 图片标题
    string description;      // 图片描述
    address author;          // 作者钱包地址
    uint256 timestamp;       // 上链时间戳
    bool exists;             // 是否存在
}
```

### 事件

- `ImageUploaded`: 图片上传成功时触发
- `ImageUpdated`: 图片信息更新时触发

## 🚀 快速开始

### 前置要求

确保已安装以下工具：
- [Node.js](https://nodejs.org/) (v16+ 推荐)
- [Ganache](https://trufflesuite.com/ganache/) (本地以太坊测试网络)
- [IPFS Desktop](https://docs.ipfs.tech/install/ipfs-desktop/) (去中心化存储)
- [MetaMask](https://metamask.io/) (浏览器钱包插件)

### 1️⃣ 环境准备

#### 安装项目依赖

```bash
# 安装后端依赖 (Truffle)
npm install

# 安装前端依赖
cd client
npm install
cd ..
```

#### 启动 Ganache

1. 打开 Ganache 应用程序
2. 点击 **"QUICKSTART"** 快速启动
3. 确认配置：
   - **RPC Server**: `http://127.0.0.1:7545`
   - **Network ID**: `5777`
4. 记录第一个账户的地址和私钥（用于 MetaMask 导入）

#### 启动 IPFS Desktop

1. 打开 IPFS Desktop
2. 等待节点启动（状态显示为绿色）
3. 确认 IPFS 运行在默认端口：
   - **API**: `http://127.0.0.1:5001`
   - **Gateway**: `http://127.0.0.1:8080`

### 2️⃣ 部署智能合约

```bash
# 编译智能合约
truffle compile

# 部署到 Ganache 本地网络
truffle migrate --network development

# 如需重新部署
truffle migrate --reset --network development
```

部署成功后，终端会显示合约地址：
```
> contract address: 0xbaEFa2bb8bCCB74EdFEaF23C2534B1fAE58f8541
```

**重要**: 复制此合约地址，需要在前端配置中使用！

### 3️⃣ 配置前端

编辑 `client/src/config/contract.js`，更新合约地址：

```javascript
export const CONTRACT_ADDRESS = "0xbaEFa2bb8bCCB74EdFEaF23C2534B1fAE58f8541"; // 你的合约地址
```

合约 ABI 会自动从 `build/contracts/ImageCopyright.json` 加载。

### 4️⃣ 配置 MetaMask

1. 打开 MetaMask 浏览器插件
2. 点击网络下拉菜单 → **"添加网络"** → **"手动添加网络"**
3. 填写网络信息：
   - **网络名称**: Ganache Local
   - **RPC URL**: `http://127.0.0.1:7545`
   - **链 ID**: `5777` (或 `1337`)
   - **货币符号**: ETH
4. 导入 Ganache 账户：
   - 点击账户图标 → **"导入账户"**
   - 粘贴 Ganache 中账户的私钥

### 5️⃣ 启动前端应用

```bash
cd client
npm run dev
```

应用将在 `http://localhost:5173` 启动。

### 6️⃣ 开始使用

1. 打开浏览器访问 `http://localhost:5173`
2. 点击 **"连接钱包"** 按钮
3. 在 MetaMask 中确认连接
4. 开始上传图片并登记版权！

## 🧪 测试

### 运行智能合约测试

```bash
# 运行所有测试
truffle test

# 运行特定测试文件
truffle test test/ImageCopyright.test.js

# 显示详细输出
truffle test --show-events
```

### 测试覆盖

测试套件包含以下场景：
- ✅ 图片上传（正常流程和异常情况）
- ✅ 数据验证（空值、重复哈希）
- ✅ 多维查询（按 ID、作者、哈希）
- ✅ 版权验证
- ✅ 信息更新（权限控制）
- ✅ 统计功能

## 💻 使用指南

### Web 界面使用

#### 1. 上传图片并登记版权

1. 点击 **"上传图片"** 标签页
2. 选择要上传的图片文件
3. 填写图片标题（必填）
4. 填写图片描述（可选）
5. 点击 **"上传并登记版权"** 按钮
6. 在 MetaMask 中确认交易
7. 等待区块链确认，成功后会显示：
   - 图片 ID
   - IPFS 哈希
   - 交易哈希

#### 2. 浏览图片库

1. 点击 **"图片库"** 标签页
2. 查看所有已登记版权的图片
3. 每张图片卡片显示：
   - 图片缩略图
   - 标题和描述
   - 作者地址
   - 上传时间
   - IPFS 哈希

#### 3. 验证图片版权

1. 点击 **"版权验证"** 标签页
2. 输入要验证的 IPFS 哈希（以 `Qm` 开头）
3. 点击 **"验证"** 按钮
4. 查看验证结果：
   - ✅ 已登记：显示版权所有者、登记时间、标题
   - ⚠️ 未登记：提示该图片未在区块链上登记

### Truffle Console 交互

对于开发者，可以使用 Truffle Console 直接与合约交互：

```bash
truffle console --network development
```

#### 常用命令示例

```javascript
// 1. 获取合约实例
let contract = await ImageCopyright.deployed()

// 2. 获取账户列表
let accounts = await web3.eth.getAccounts()

// 3. 上传图片版权信息
await contract.uploadImage(
  "QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG",
  "我的照片",
  "这是一张美丽的风景照",
  { from: accounts[0] }
)

// 4. 查询所有图片
let images = await contract.getAllImages()
console.log(images)

// 5. 查询特定图片
let image = await contract.getImage(1)
console.log(image)

// 6. 查询我的图片
let myImages = await contract.getImagesByAuthor(accounts[0])
console.log(myImages)

// 7. 验证图片哈希
let exists = await contract.verifyImageHash("QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG")
console.log("图片是否存在:", exists)

// 8. 根据哈希获取版权信息
let copyrightInfo = await contract.getImageByHash("QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG")
console.log("版权所有者:", copyrightInfo.author)
console.log("登记时间:", new Date(copyrightInfo.timestamp * 1000))

// 9. 更新图片信息（仅作者）
await contract.updateImageInfo(1, "新标题", "新描述", { from: accounts[0] })

// 10. 获取统计信息
let stats = await contract.getStats()
console.log("总图片数:", stats.totalImages.toString())
console.log("我的图片数:", stats.userImages.toString())
```

### 使用交互脚本

项目提供了交互脚本演示所有功能：

```bash
truffle exec scripts/interact.js --network development
```

## 📚 IPFS 使用指南

### 通过 Web 界面上传

前端应用已集成 IPFS 上传功能，选择图片后会自动上传到 IPFS。

### 手动上传到 IPFS

#### 方法 1: IPFS Desktop

1. 打开 IPFS Desktop
2. 点击左侧 **"Files"** 标签
3. 点击右上角 **"Import"** → **"File"**
4. 选择要上传的图片
5. 上传成功后，点击文件右侧的 **"..."** 菜单
6. 选择 **"Copy CID"** 复制 IPFS 哈希
7. 使用此 CID 在应用中登记版权

#### 方法 2: IPFS 命令行

```bash
# 上传文件
ipfs add your-image.jpg

# 输出示例
# added QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG your-image.jpg
```

### 访问 IPFS 图片

- **本地网关**: `http://localhost:8080/ipfs/<CID>`
- **公共网关**:
  - `https://ipfs.io/ipfs/<CID>`
  - `https://cloudflare-ipfs.com/ipfs/<CID>`
  - `https://gateway.pinata.cloud/ipfs/<CID>`

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                         用户界面                             │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  上传图片    │  │   图片库     │  │  版权验证    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    前端应用层 (React)                        │
│                                                              │
│  ┌───────────────────┐        ┌───────────────────┐        │
│  │  UI 组件          │        │  业务逻辑服务      │        │
│  │  - UploadImage    │◄──────►│  - copyrightService│        │
│  │  - ImageGallery   │        │  - 上传/查询/验证  │        │
│  │  - VerifyCopyright│        └─────────┬─────────┘        │
│  └───────────────────┘                  │                   │
│                                          ▼                   │
│  ┌─────────────────────────────────────────────────┐       │
│  │            工具层 (Utils)                        │       │
│  │  ┌──────────────────┐    ┌──────────────────┐  │       │
│  │  │  合约交互工具    │    │   IPFS 工具      │  │       │
│  │  │  - connectWallet │    │  - uploadToIPFS  │  │       │
│  │  │  - getContract   │    │  - getIPFSUrl    │  │       │
│  │  └──────────────────┘    └──────────────────┘  │       │
│  └─────────────────────────────────────────────────┘       │
└────────────────────┬────────────────────┬───────────────────┘
                     │                    │
                     ▼                    ▼
        ┌────────────────────┐  ┌──────────────────┐
        │   区块链层          │  │   存储层          │
        │                    │  │                  │
        │ ┌────────────────┐ │  │ ┌──────────────┐ │
        │ │ ImageCopyright │ │  │ │     IPFS     │ │
        │ │  智能合约       │ │  │ │  分布式存储   │ │
        │ └────────────────┘ │  │ └──────────────┘ │
        │                    │  │                  │
        │    Ganache         │  │   IPFS Desktop   │
        │   (测试网络)        │  │                  │
        └────────────────────┘  └──────────────────┘
```

## 🔧 开发指南

### 合约开发

#### 修改智能合约

1. 编辑 `contracts/ImageCopyright.sol`
2. 编译合约: `truffle compile`
3. 运行测试: `truffle test`
4. 重新部署: `truffle migrate --reset --network development`

#### 添加新功能

编写测试 → 实现功能 → 运行测试 → 更新文档

### 前端开发

#### 目录说明

- `components/`: React 组件（UI 层）
- `services/`: 业务逻辑服务（与合约交互）
- `utils/`: 工具函数（合约连接、IPFS 操作）
- `config/`: 配置文件（合约地址、ABI）

#### 开发流程

```bash
cd client
npm run dev        # 启动开发服务器
npm run build      # 构建生产版本
npm run preview    # 预览生产构建
```

#### 添加新组件

1. 在 `src/components/` 创建组件文件
2. 在 `App.jsx` 中引入并使用
3. 遵循现有的样式规范（TailwindCSS）

### 合约 Gas 优化建议

- ✅ 使用 `mapping` 而非 `array` 存储数据
- ✅ 只存储必要的数据（IPFS 哈希，不存储图片本身）
- ✅ 批量操作优于单次操作
- ✅ 合理使用 `view`/`pure` 函数（不消耗 gas）
- ✅ 事件比存储便宜，用于历史记录

## 📖 智能合约优化特性

1. **防重复上传**: 使用 `mapping(string => bool)` 检测 IPFS 哈希是否已存在
2. **作者索引**: 通过 `mapping(address => uint256[])` 快速查询作者的所有作品
3. **版权验证**: 提供 `getImageByHash()` 根据 IPFS 哈希查询原作者
4. **事件日志**: 完整的 `ImageUploaded` 和 `ImageUpdated` 事件便于前端监听
5. **权限控制**: `updateImageInfo()` 仅作者可更新自己的作品信息
6. **输入验证**: 完善的 `require` 语句确保数据完整性
7. **Gas 优化**: 使用 Solidity 0.8.21 优化器，200 runs

## 🌍 部署到测试网/主网

### 部署到公共测试网（如 Sepolia）

1. 安装 HDWallet Provider:
```bash
npm install @truffle/hdwallet-provider
```

2. 创建 `.env` 文件:
```
MNEMONIC="your twelve word mnemonic here"
INFURA_API_KEY="your_infura_api_key"
```

3. 在 `truffle-config.js` 中添加网络配置:
```javascript
const HDWalletProvider = require('@truffle/hdwallet-provider');
require('dotenv').config();

module.exports = {
  networks: {
    sepolia: {
      provider: () => new HDWalletProvider(
        process.env.MNEMONIC,
        `https://sepolia.infura.io/v3/${process.env.INFURA_API_KEY}`
      ),
      network_id: 11155111,
      gas: 4000000,
      gasPrice: 10000000000,
    }
  }
};
```

4. 部署:
```bash
truffle migrate --network sepolia
```

### 前端生产构建

```bash
cd client
npm run build
```

构建产物在 `client/dist/` 目录，可部署到：
- Vercel
- Netlify
- GitHub Pages
- IPFS (完全去中心化！)

## ❓ 常见问题

### Q1: 部署失败，提示 "network not found"

**A**: 确保 Ganache 正在运行，且端口为 7545。检查 `truffle-config.js` 中的配置。

### Q2: MetaMask 无法连接

**A**: 
1. 确认已添加 Ganache 本地网络
2. 检查链 ID 是否正确（5777 或 1337）
3. 尝试重置 MetaMask 账户（设置 → 高级 → 重置账户）

### Q3: 前端无法读取合约

**A**: 
1. 确认合约已部署成功
2. 检查 `client/src/config/contract.js` 中的合约地址是否正确
3. 确认 ABI 是否正确（应从 `build/contracts/ImageCopyright.json` 复制）

### Q4: IPFS 图片无法访问

**A**: 
- 确保 IPFS Desktop 正在运行
- 本地网关: `http://localhost:8080/ipfs/<CID>`
- 如果使用公共网关，第一次访问可能需要几分钟
- 考虑使用 Pinata 或 Infura 等 IPFS Pinning 服务

### Q5: 交易失败，提示 "out of gas"

**A**: 
在 Ganache 中，gas 限制通常足够。如果遇到此问题：
1. 检查合约代码是否有死循环
2. 在 `truffle-config.js` 中增加 gas limit

### Q6: 如何重新部署合约？

**A**: 
```bash
truffle migrate --reset --network development
```

**注意**: 重新部署后需要更新前端配置中的合约地址！

### Q7: 如何在主网部署？

**A**: 主网部署需要真实的 ETH 支付 gas 费用，建议：
1. 先在测试网（Sepolia、Goerli）充分测试
2. 进行安全审计
3. 优化 gas 消耗
4. 准备足够的 ETH（gas 费）
5. 使用硬件钱包管理私钥

## 📋 后续开发路线图

### 短期目标
- [x] 基础版权存证功能
- [x] React Web 界面
- [x] 完整测试套件
- [ ] 图片缩略图生成
- [ ] 批量上传功能
- [ ] 搜索和过滤功能

### 中期目标
- [ ] 版权转让功能
- [ ] 版权许可机制（Creative Commons）
- [ ] 多签名授权
- [ ] 水印添加功能
- [ ] 移动端应用（React Native）

### 长期目标
- [ ] NFT 集成（ERC-721/ERC-1155）
- [ ] 版权交易市场
- [ ] 版权纠纷仲裁机制
- [ ] AI 图片相似度检测
- [ ] 收益分配智能合约
- [ ] 跨链支持

## ⚠️ 安全注意事项

1. **私钥安全**: 
   - 永远不要在代码中硬编码私钥
   - 使用 `.env` 文件并添加到 `.gitignore`
   - 主网部署使用硬件钱包（Ledger、Trezor）

2. **智能合约安全**:
   - 部署前进行安全审计
   - 遵循 Checks-Effects-Interactions 模式
   - 使用 OpenZeppelin 等经过审计的库
   - 防止重入攻击

3. **Gas 费用**:
   - 主网部署前充分测试 gas 消耗
   - 设置合理的 gas price
   - 避免在合约中存储大量数据

4. **IPFS 持久化**:
   - 本地 IPFS 节点可能不稳定
   - 使用 Pinning 服务（Pinata、Infura）确保文件持久化
   - 考虑多节点备份

5. **前端安全**:
   - 验证用户输入
   - 防止 XSS 攻击
   - 使用 HTTPS
   - 实施 Content Security Policy (CSP)

## 📚 参考资源

### 官方文档
- [Solidity](https://docs.soliditylang.org/)
- [Truffle Suite](https://archive.trufflesuite.com/docs/truffle/)
- [Ethers.js](https://docs.ethers.org/)
- [React](https://react.dev/)
- [IPFS](https://docs.ipfs.tech/)
- [MetaMask](https://docs.metamask.io/)

### 学习资源
- [CryptoZombies](https://cryptozombies.io/) - Solidity 交互式教程
- [Ethereum.org](https://ethereum.org/zh/developers/) - 以太坊开发者门户
- [OpenZeppelin](https://docs.openzeppelin.com/) - 智能合约库
- [Web3.University](https://www.web3.university/) - Web3 开发课程

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

### 提交 PR 前
1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

### 代码规范
- Solidity: 遵循 [Solidity Style Guide](https://docs.soliditylang.org/en/latest/style-guide.html)
- JavaScript: 使用 ESLint 配置
- Git Commit: 使用语义化提交信息

## 📄 License

本项目基于 MIT 许可证开源 - 详见 [LICENSE](LICENSE) 文件

## 👨‍💻 作者

如有问题或建议，欢迎联系！

---

**⭐ 如果这个项目对你有帮助，请给个 Star！**
